<nav class="navbar sticky-top navbar-expand-lg navbar-light bg-light">
    <div class="mx-auto order-0">
        <a class="navbar-brand mx-auto" href="{{ path('pattern_index') }}">news from my couch</a>
    </div>
    {% if is_granted('ROLE_ADMIN') %}
        <button class="navbar-toggler" type="button"
                data-toggle="collapse" data-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto">
                <li {% if route_name == "pattern_index" %} class="nav-item active"
                {% else %} class="nav-item" {% endif %}>
                    <a class="nav-link" href="{{ path('pattern_index') }}">Patrons</a>
                </li>
                <li {% if route_name == "category_index" %} class="nav-item dropdown active"
                {% else %} class="nav-item dropdown" {% endif %}>
                    <a class="nav-link" href="{{ path('category_index') }}">Catégories</a>
                </li>
                <li {% if route_name == "yarn_index" %} class="nav-item active"
                {% else %} class="nav-item" {% endif %}>
                    <a class="nav-link" href="{{ path('yarn_index') }}">Fils</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">
                        <img src="{{ asset('uploads/icons/logout.png') }}" alt="logout_icon" width="25">
                    </a>
                </li>
            </ul>
        </div>
    {% endif %}
</nav>