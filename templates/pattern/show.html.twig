{% extends 'base.html.twig' %}

{% block title %}Pattern{% endblock %}

{% block body %}
    <h1>{{ pattern.category.name }} {{ pattern.name }}</h1>
    <ul>
        <li>{{ pattern.description }}</li>
        <li>Difficulty : {{ pattern.difficulty }}</li>
    </ul>
    {% for yarn in pattern.yarns %}
        <p>{{ yarn.name }}</p>
    {% endfor %}
{% endblock %}
