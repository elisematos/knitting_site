{% extends 'base.html.twig' %}

{% block title %}Pattern index{% endblock %}

{% block body %}
    <a class="btn btn-dark" href="{{ path('pattern_new') }}">Ajouter un nouveau patron</a>
    <h1>Pattern index</h1>

    {% for pattern in patterns %}
        <a href="{{ path('pattern_show', {'id': pattern.id}) }}">{{ pattern.name }}</a>
        <a href="{{ path('pattern_edit', {'id': pattern.id}) }}">Editer</a>
        {{ include('pattern/_delete_form.html.twig') }}
    {% else %}
        Aucun patron retrouvé
    {% endfor %}
{% endblock %}
