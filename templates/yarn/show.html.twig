{% extends 'base.html.twig' %}

{% block title %}Yarn{% endblock %}

{% block body %}
    <h1>Yarn</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ yarn.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ yarn.name }}</td>
            </tr>
            <tr>
                <th>Weight</th>
                <td>{{ yarn.weight }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('yarn_index') }}">back to list</a>

    <a href="{{ path('yarn_edit', {'id': yarn.id}) }}">edit</a>

    {{ include('yarn/_delete_form.html.twig') }}
{% endblock %}
